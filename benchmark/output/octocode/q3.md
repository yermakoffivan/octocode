### Q3 — `revalidatePath` call sites [drift]

In `vercel/next.js`, find every call to `revalidatePath` inside `packages/next/src/server/`.
For each match: state the file path, line number, and the exact source line.

> *Tests search completeness and match-context quality. Judge verifies that line numbers are correct and that returned source lines match the actual file. D=3 requires exact source lines for every match, not paraphrases. Judge independently verifies the total count.*

---

## Metadata

| Field           | Value |
|-----------------|-------|
| Run             | octocode |
| Agent           | octocode |
| Model           | claude-sonnet-4-5 |
| Recorded        | 2026-06-13T22:15:17Z |
| Calls           | 7 |
| In Chars        | 4229 |
| Out Chars       | 48234 |
| Tool elapsed ms | 9257 |
| Q elapsed ms    | 146225 |

## Answer

GitHub code search for `revalidatePath` within `packages/next/src/server/` returned 7 files. After reading each file, the results for actual calls (invocations) are:

- `packages/next/src/server/web/spec-extension/revalidate.ts` — **definition** of `revalidatePath`. No other call in this file.
  - The function is exported at approximately line 104: `export function revalidatePath(originalPath: string, type?: 'layout' | 'page') {`
  - Within the function body the string literal `` `revalidatePath ${originalPath}` `` is passed to the internal `revalidate()` helper — not a self-call.
- `packages/next/src/server/lib/router-utils/cache-life-type-utils.ts` line 176 — **re-export** only (`export { revalidatePath }` inside `declare module 'next/cache'`), not an invocation.
- `packages/next/src/server/lib/router-utils/cache-life-type-utils.test.ts` lines 28, 186, 264, 341, 419 — all inside `declare module` re-export blocks (type fixture for tests), not invocations.
- `packages/next/src/server/lib/implicit-tags.ts` line 30 — comment reference only: `// for revalidatePath handling`.
- `packages/next/src/server/app-render/action-handler.ts` line 1348 — comment only: `// If the action called revalidateTag/revalidatePath, then that might`.
- `packages/next/src/server/lib/encode-cache-tag.ts` — comment reference only.

**Conclusion (drift):** As of the canary branch, there are **no actual call-site invocations** of `revalidatePath(...)` within `packages/next/src/server/` outside its own definition file. The function is defined in `packages/next/src/server/web/spec-extension/revalidate.ts` and exported for user/framework consumption; the server code itself does not call it internally within this directory.
