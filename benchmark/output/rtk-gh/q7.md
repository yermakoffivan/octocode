### Q7 — How does `revalidateTag` invalidate cached data?

In `vercel/next.js`, trace how calling `revalidateTag(tag)` invalidates cached entries end-to-end.
1. Where is `revalidateTag` defined on the server side? State the exact file path and line number.
2. What data structure does it write to when called? Quote the relevant source line(s).
3. Where does the server read that structure to decide what to revalidate? State the file path and function name.

> *Tests multi-hop architecture tracing through the cache invalidation pipeline. D=3 requires file:line for the definition, a verbatim quote of the write operation, and the correct consumer location. Agents that find only the public export without tracing the cache store write + consumer read score D≤1.*

---

## Metadata

| Field           | Value |
|-----------------|-------|
| Run             | rtk-gh |
| Agent           | rtk-gh |
| Model           | claude-sonnet-4-6 |
| Recorded        | 2026-06-13T22:39:44Z |
| Calls           | 4 |
| In Chars        | 421 |
| Out Chars       | 3102 |
| Tool elapsed ms | 326 |
| Q elapsed ms    | 45784 |

## Answer

- Server-side `revalidateTag` is defined in `packages/next/src/server/web/spec-extension/revalidate.ts` at line 34: `export function revalidateTag(tag: string, profile: string | CacheLifeConfig)`.
- When called it writes to the work-store's `pendingRevalidatedTags` array: `store.pendingRevalidatedTags.push({ tag, profile, })` (lines 224–227 in the same file, inside `function revalidate`). The array is lazily initialized at line 207–208: `if (!store.pendingRevalidatedTags) { store.pendingRevalidatedTags = [] }`.
- Consumer: `executeRevalidates` in `packages/next/src/server/revalidation-utils.ts` (line 186) reads `state?.pendingRevalidatedTags ?? workStore.pendingRevalidatedTags ?? []` (lines 192–193) and passes them to `revalidateTags(pendingRevalidatedTags, workStore.incrementalCache, workStore)` (lines 196–200).
