// CLI command specs are sourced from octocode-core (the single source of truth
// for tool/command resources, exposed via `@octocodeai/octocode-core/cli`).
// Do NOT redefine them here — that would duplicate the canonical specs.
export { COMMAND_SPECS, findCommandSpec } from '@octocodeai/octocode-core/cli';
