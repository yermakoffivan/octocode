# chromium — Repo Benchmark Results

> Not yet run. Execute `yarn repo:clone && yarn repo:bench chromium` to populate.
>
> Note: chromium clone fetches `base/` only (sparse checkout). First clone takes ~5–10 min.
