# CLI Tools & Flow Benchmark Recipe

The canonical CLI, raw-tool, OQL, and workflow question catalog now lives at
[`benchmark/octocode/`](https://github.com/bgauryy/octocode/blob/main/packages/octocode-benchmark/benchmark/octocode/README.md).

Keep this file as a stable compatibility link for older references. Do not add
flow rows here; add new CLI/tool/OQL/workflow coverage to the split catalog so
the benchmark package has one source of truth and no duplicated flows.

For deterministic run output, use the
[`agent benchmark runbook`](https://github.com/bgauryy/octocode/blob/main/packages/octocode-benchmark/recipes/agent-benchmark-runbook.md).
