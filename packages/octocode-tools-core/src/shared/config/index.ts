// Only real tools-core config code lives here.
// All config types, defaults, resolver, and validation logic moved to @octocodeai/config.
// Callers that need config logic import directly from '@octocodeai/config'.
export { OctocodeConfigSchema } from './schemas.js';
